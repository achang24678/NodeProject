var person = {
  name: 'Andrew'
};

person.age = 25;

debugger; // press c to hit this break here in debugging mode, then we can press c to run through the other half

person.name = 'Mike';

console.log(person);
